# Project images

Put the original collection image files in this folder, then add standard `<img>` elements to `../work.html`.

The Cargo media hashes alone do not include enough information to recover or download the original files.
